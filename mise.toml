experimental_monorepo_root = true

[tools]
go = "latest"
golangci-lint = "2.7.1" # Freeze since I'm not actively developing go
rust = { version = "stable", profile = "default" }

[tasks.check-all]
depends = ["//2024:check", "//2025:check"]
